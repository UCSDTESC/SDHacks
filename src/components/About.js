import React from 'react';
import { Container, Row, Col } from 'react-bootstrap';

const About = () => {
    return (
        <section>
            <Container>Write your about page in here!</Container>
        </section>
    );
};

export default About;
